// torch_helper_stub.dart
import 'package:flutter/cupertino.dart';

void debugImport() {
  debugPrint("📱 Import de torch_helper_stub.dart (version mobile) !");
}

Future<void> toggleTorchWeb(bool enable) async {
  debugPrint("❌ La torche Web n'est pas supportée sur cette plateforme.");
}

Future<void> disableTorchWeb() async {
  debugPrint("❌ La torche Web n'est pas supportée sur cette plateforme.");
}